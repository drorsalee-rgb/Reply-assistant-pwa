{
  "name": "beacon-webhook",
  "version": "0.1.0",
  "private": true,
  "description": "Receives inbound WhatsApp messages from Beacon: Fake Hunting opt-ins and network preferences",
  "main": "src/index.js",
  "scripts": { "start": "node src/index.js" },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@google-cloud/firestore": "^7.10.0",
    "express": "^4.21.0"
  }
}
