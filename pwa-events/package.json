{
  "name": "pwa-events",
  "version": "0.1.0",
  "private": true,
  "description": "Aggregate, privacy-preserving usage counters for the Yoriki Light PWA",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.10.0",
    "express": "^4.21.0"
  }
}
