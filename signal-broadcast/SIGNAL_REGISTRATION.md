# רישום הבוט בסיגנל — yoriki-500811

מדריך לביצוע מול מי שמחזיק את ה-SIM של **052-434-2846** (`+972524342846`).
כל השאר כבר מוכן; זה השלב היחיד שדורש נוכחות אנושית ליד המכשיר.

## לפני שמתחילים — לוודא מול מחזיק ה-SIM

1. **המכשיר בהישג יד** ויכול לקבל SMS למספר הזה.
2. **האם המספר כבר רשום בסיגנל?** אם כן, רישום כבוט **ינתק את החשבון הקיים**
   מהטלפון. צריך שהוא יאשר את זה מראש.
3. **הוואטסאפ לא ייפגע.** סיגנל ווואטסאפ נפרדים לחלוטין, וחיבור ה-Beacon ימשיך
   לעבוד. חשוב רק שה-SIM יישאר במכשיר שמחזיק את חיבור הוואטסאפ.

## הסביבה (כבר קיימת)

- VM `signal-cli` בזון `europe-west1-b`, IP פנימי `10.132.0.2`
- signal-cli-rest-api רץ במצב `json-rpc`, נתונים ב-`/var/signal-cli-data`
- אין עדיין אף חשבון רשום (`/v1/accounts` מחזיר `[]`)

## השלבים

### 1. פתיחת מנהרה ל-VM

```bash
gcloud compute ssh signal-cli --project=yoriki-500811 --account=saleeclaude@gmail.com \
  --zone=europe-west1-b --tunnel-through-iap
```

### 2. בקשת קוד אימות

מתוך ה-VM:

```bash
curl -X POST -H "Content-Type: application/json" \
  http://127.0.0.1:8080/v1/register/+972524342846
```

- **הצלחה:** אין פלט, ו-SMS נשלח למכשיר.
- **אם מתקבל captcha required:** צריך לפתוח את
  `https://signalcaptchas.org/registration/generate.html` בדפדפן, לפתור,
  להעתיק את הטוקן (מתחיל ב-`signalcaptcha://`) ולשלוח שוב עם
  `{"captcha":"<token>"}` בגוף הבקשה.

### 3. אימות הקוד שהתקבל ב-SMS

```bash
curl -X POST -H "Content-Type: application/json" \
  http://127.0.0.1:8080/v1/register/+972524342846/verify/123456
```

(להחליף `123456` בקוד האמיתי.)

### 4. אימות שהרישום עבד

```bash
curl -s http://127.0.0.1:8080/v1/accounts
```

צריך להחזיר רשימה שמכילה את `+972524342846`.

## אחרי הרישום

1. **להוסיף את הבוט החדש לקבוצות הסיגנל.** הקבוצות הקיימות מכילות את הבוט
   **הישן** (`+972559761823`) — הן לא יעבדו עם החדש.
2. **לעדכן `content_providers.signal_group_id`** בפיירסטור של `yoriki-500811`
   כך שיצביע על קבוצות שהבוט החדש חבר בהן.
3. **להריץ Test B** (מסלול הקבוצות) — ורק אז לשקול לחבר את הקוראים.

## חשוב

- **לא להעתיק** את `/var/signal-cli-data` מה-VM הישן.
- **לא להריץ** את אותו לוגין בשתי מכונות signal-cli חיות במקביל.
- הפרודקשן הישן ב-`torino-social` ממשיך לרוץ עם הבוט הישן ולא מושפע.
