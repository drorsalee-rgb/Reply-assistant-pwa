{
  "name": "signal-broadcast",
  "version": "0.1.0",
  "private": true,
  "description": "Receives broadcast triggers and sends Signal messages containing reply-assistant PWA links",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.10.0",
    "express": "^4.21.0"
  }
}
